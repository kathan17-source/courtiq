# Backend database layer

This folder is reserved for SQLAlchemy models, sessions and migrations.

Current schema source of truth:

```text
backend/database/schema.sql
```

Next step: add Alembic migrations and typed database repositories.
